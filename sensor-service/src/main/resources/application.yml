spring:
    application:
        name: sensor-service
    config:
        import: optional:configserver:http://localhost:8888

feign:
    client:
        config:
            default:
                connectTimeout: 5000
                readTimeout: 5000
